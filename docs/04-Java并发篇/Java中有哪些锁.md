# Java中有哪些锁

Java中的锁是用来解决一个核心问题的，即线程安全问题。

线程安全问题涉及到，原子性、可见性以及有序性

从功能层面上看，锁可以分为两大类：
- 共享锁（读锁）
- 排他锁（写锁）

从编程能力上看，锁又可以分成两大类：
- 悲观锁
- 乐观锁（AQS的基础——> 原子性、可见性）

锁升级：偏向锁、轻量级锁、重量级锁

锁的特性：
- 可重入锁（防止死锁的一种设计）
- 不可重入锁


从底层看，锁又可以分成两大类：
- 公平锁
- 非公平锁（减少线程的阻塞唤醒）


锁消除、锁膨胀