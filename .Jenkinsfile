// jenkins流水线

pipeline {
  agent any

  stages {
    stage("拉去代码") {
      steps {
        echo "拉取中"
        sh "ls -al"
      }
    }

    stage("构建项目") {
      steps {
        echo "构建中"
        sh "ls -al"
      }
    }
  }
}